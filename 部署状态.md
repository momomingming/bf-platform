# bf-platform 部署状态

## 🔗 访问地址

**http://82.156.43.223:8501**

---

## 服务器信息

| 项目 | 值 |
|---|---|
| 云厂商 | 腾讯云轻量应用服务器（Lighthouse） |
| 实例 ID | `lhins-gkzr2xzg` |
| 地域 | ap-beijing-3 |
| 公网 IP | 82.156.43.223 |
| 配置 | 2C2G + 40GB SSD |
| 系统 | Windows Server（宝塔面板） |
| 到期 | 2026-10-02（已设 09-29 提醒） |

---

## 运行状态

| 项目 | 值 |
|---|---|
| Windows 服务 | `BFPlatform` |
| 启动类型 | 自动（SERVICE_AUTO_START） |
| 崩溃恢复 | 5 秒后自动重启（nssm AppRestartDelay） |
| 应用目录 | `C:\apps\bf-platform\lis_battery_platform` |
| 日志目录 | `C:\apps\bf-platform\logs\` |
| 监听 | `0.0.0.0:8501` |
| 健康检查 | `/_stcore/health` → `ok` |

---

## 已安装依赖

Python 3.11.9（`C:\Python311\python.exe`）

| 包 | 版本 |
|---|---|
| streamlit | 1.63.0 |
| plotly | 7.0.0 |
| pandas | 3.0.5 |
| numpy | 2.4.6 |
| scikit-learn | 1.9.0 |
| xgboost | 3.2.0 |
| scipy | 1.17.1 |
| altair / pydeck / kaleido | 已装 |

---

## 如何更新部署

仓库已转**公开**，云主机可直接走 gh-proxy 镜像高速下载（云主机**未装 Git**，直接下 zip 覆盖）：

```powershell
# 1. 停服务
& C:\tools\nssm\nssm.exe stop BFPlatform

# 2. 下载主分支最新代码（约 2 秒）
Invoke-WebRequest `
  -Uri 'https://gh-proxy.com/https://codeload.github.com/momomingming/bf-platform/zip/refs/heads/main' `
  -OutFile 'C:\temp\bf.zip' -UseBasicParsing
Expand-Archive -Path 'C:\temp\bf.zip' -DestinationPath 'C:\apps\_new' -Force

# 3. 覆盖（不用 /MIR，避免误删 logs）
robocopy 'C:\apps\_new\bf-platform-main' 'C:\apps\bf-platform' /E /R:1 /W:1
Remove-Item C:\apps\_new -Recurse -Force

# 4. 启动
& C:\tools\nssm\nssm.exe start BFPlatform
```

> release zip（`v1.0.0`）顶层目录是 `bf-platform/`，可直接 `Expand-Archive` 到 `C:\apps`；
> codeload zip 顶层是 `bf-platform-main/`，需 robocopy 一层。

**更新后自检（可选，约 1~2 分钟）**：

```powershell
cd C:\apps\bf-platform\lis_battery_platform
C:\Python311\python.exe -c "from streamlit.testing.v1 import AppTest;at=AppTest.from_file('app.py',default_timeout=900);at.run();print('LIQ_EXC',len(at.exception));at.radio[0].set_value('固态锂硫电池');at.run();print('SOL_EXC',len(at.exception))"
```

输出 `LIQ_EXC 0` / `SOL_EXC 0` 即液态、固态两条路径都无异常。

---

## 常用运维命令

```powershell
# 服务状态
Get-Service BFPlatform

# 启停
& C:\tools\nssm\nssm.exe start BFPlatform
& C:\tools\nssm\nssm.exe stop BFPlatform
& C:\tools\nssm\nssm.exe restart BFPlatform

# 看日志
Get-Content C:\apps\bf-platform\logs\streamlit.log -Tail 50
Get-Content C:\apps\bf-platform\logs\streamlit.err.log -Tail 50

# 健康检查
Invoke-WebRequest http://127.0.0.1:8501/_stcore/health -UseBasicParsing
```

---

## GitHub 仓库

**https://github.com/momomingming/bf-platform**（公开）

- 首个 release：`v1.0.0`
- 更新方式：本地改 → `git push` → 打新 release → 云主机下载覆盖

---

## 修复记录

### 2026-09-09 · 首屏 `ValueError: could not convert string to float: 'MnO₂'`

- **根因**：`modeling.py::encode_categoricals` 用 `df[col].dtype == 'object'` 判断分类列。
  pandas **3.0** 起字符串列默认 dtype 是 `str`（PDEP-14），不再是 `object`，
  于是 `填料类型 / 填料类别 / 基底材料 / 电解液体系 / 基底聚合物` 全部漏编码，
  到 `astype(float)` 时炸。
- **修复**：改用 `pandas.api.types.is_numeric_dtype` 判断；`train_models` 里
  再对 `to_numeric(coerce)` 后仍为 NaN 的列补一次 LabelEncoder 兜底；
  目标列同样走 `to_numeric` + 均值兜底。
- **验证**：本地（pandas 3.0.5 + streamlit AppTest）与云主机各跑一遍，
  液态 / 固态均 `0 异常`；http://82.156.43.223:8501 健康检查 200。
- 提交：`20ae2c9`

---

## 已知问题

1. **Windows 防火墙规则未添加成功**
   `New-NetFirewallRule` 报 Error 1753（RPC endpoint mapper 问题）。
   不影响访问——Lighthouse 防火墙已放行 8501，外部实测 HTTP 200。
   如需补：`netsh advfirewall firewall add rule name="BF8501" dir=in action=allow protocol=TCP localport=8501`

2. **训练数据仍为合成数据**
   `generate_data.py` 生成（液态 650 条 + 固态 600 条）。
   Phase 2 计划接入真实 .cex 实验数据。

3. **实例 2026-10-02 到期**，需在到期前续费，否则平台下线。
